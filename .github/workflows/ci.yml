# CI for @browsercore/fetch — typecheck, lint, test, coverage, build, publish.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-publish:
    uses: jverneuer/browsercore-dev/.github/workflows/reusable-ci.yml@main
    with:
      node-version: '26'
    permissions:
      contents: write
      id-token: write
      pages: write
